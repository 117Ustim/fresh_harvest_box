# 📚 ШАГ 8: Использование админ-панели

Теперь научимся использовать админ-панель на полную мощность!

---

## 📄 Часть 1: Добавление админ-панели на другие страницы

### Пример: Страница "О нас"

#### Шаг 8.1.1: Создайте файл app/about/page.js

Создайте папку `about` внутри `app`, затем файл `page.js`:

```
ваш-проект/
├── app/
│   ├── about/
│   │   └── page.js  ← СОЗДАЙТЕ ЭТОТ ФАЙЛ
│   └── page.js
```

#### Шаг 8.1.2: Вставьте код

```javascript
'use client';

import { AdminPanel, useContent } from '../../universal-admin-package/src/index.js';

function AboutContent() {
  // Получаем данные страницы "about"
  const header = useContent('pages.about.header');
  
  if (!header) {
    return <div>Загрузка...</div>;
  }
  
  return (
    <div style={{ padding: '50px' }}>
      <h1>{header.title}</h1>
      <p>{header.description}</p>
    </div>
  );
}

export default function AboutPage() {
  return (
    <main>
      <AdminPanel />
      <AboutContent />
    </main>
  );
}
```

#### Шаг 8.1.3: Откройте страницу

Перейдите в браузере: `http://localhost:3000/about`

Вы увидите:
- Заголовок: "О нашей компании"
- Описание
- Кнопку админ-панели

---

## 🆕 Часть 2: Создание новой страницы через админ-панель

### Шаг 8.2.1: Откройте админ-панель

Нажмите кнопку **"⚙️ Админ-панель"**

### Шаг 8.2.2: Нажмите "Создать страницу"

В верхней части админ-панели найдите кнопку:
```
+ Создать страницу
```

### Шаг 8.2.3: Введите ID страницы

Появится окно. Введите ID новой страницы, например:
```
services
```

⚠️ **Важно:** Используйте только английские буквы, без пробелов!

Нажмите **"Создать"**

### Шаг 8.2.4: Добавьте секцию

1. Откроется пустая страница
2. Нажмите **"+ Добавить секцию"**
3. Введите название секции, например: `header`
4. Нажмите **"Создать"**

### Шаг 8.2.5: Добавьте поля

1. Откройте секцию `header`
2. Нажмите **"+ Добавить поле"**
3. Введите название поля: `title`
4. Выберите тип: **text**
5. Нажмите **"Создать"**

Повторите для других полей (description, image и т.д.)

### Шаг 8.2.6: Заполните данные

Введите значения в созданные поля и нажмите **"Сохранить"**

---

## 🎨 Часть 3: Типы полей и как их использовать

### 1. Текстовое поле (text)

**Для чего:** Короткие тексты (заголовки, имена)

**Как создать в database.example.js:**
```javascript
title: "Мой заголовок"
```

**Как использовать в коде:**
```javascript
const hero = useContent('pages.home.hero');
<h1>{hero.title}</h1>
```

---

### 2. Длинный текст (textarea)

**Для чего:** Длинные тексты (описания, статьи)

**Как создать:**
```javascript
description: "Очень длинный текст с подробным описанием нашей компании и всех услуг которые мы предоставляем..."
```

**Как использовать:**
```javascript
<p>{hero.description}</p>
```

---

### 3. Изображение (image)

**Для чего:** Одно изображение (логотип, фото)

**Как создать:**
```javascript
logo: "/images/logo.png"
// или
photo: ""  // Пустое - загрузите через админку
```

**Как использовать:**
```javascript
{hero.logo && <img src={hero.logo} alt="Логотип" />}
```

---

### 4. Галерея изображений (image-array)

**Для чего:** Несколько изображений (галерея, слайдер)

**Как создать:**
```javascript
gallery: {
  images: [
    "/photo1.jpg",
    "/photo2.jpg"
  ]
}
```

**Как использовать:**
```javascript
const gallery = useContent('pages.home.gallery');

{gallery?.images?.map((img, i) => (
  <img key={i} src={img} alt={`Фото ${i + 1}`} />
))}
```

---

### 5. Массив объектов (repeater)

**Для чего:** Список элементов (услуги, товары, команда)

**Как создать:**
```javascript
features: {
  items: [
    {
      icon: "/icon1.svg",
      title: "Быстро",
      description: "Выполняем работу в срок"
    },
    {
      icon: "/icon2.svg",
      title: "Качественно",
      description: "Высокие стандарты"
    }
  ]
}
```

**Как использовать:**
```javascript
const features = useContent('pages.home.features');

{features?.items?.map((item, i) => (
  <div key={i}>
    <img src={item.icon} alt={item.title} />
    <h3>{item.title}</h3>
    <p>{item.description}</p>
  </div>
))}
```

---

## 🔧 Часть 4: Полезные советы

### Совет 1: Получение одного поля

Вместо получения всей секции:
```javascript
const hero = useContent('pages.home.hero');
<h1>{hero.title}</h1>
```

Можно получить только нужное поле:
```javascript
const title = useContent('pages.home.hero.title');
<h1>{title}</h1>
```

---

### Совет 2: Значение по умолчанию

Если данных еще нет, покажите текст по умолчанию:
```javascript
const title = useContent('pages.home.hero.title', {
  defaultValue: 'Заголовок по умолчанию'
});
```

---

### Совет 3: Без автообновления

Если не нужно автоматическое обновление (для статичных данных):
```javascript
const title = useContent('pages.home.hero.title', {
  subscribe: false
});
```

---

### Совет 4: Проверка на существование

Всегда проверяйте что данные загрузились:
```javascript
const hero = useContent('pages.home.hero');

if (!hero) {
  return <div>Загрузка...</div>;
}

return <h1>{hero.title}</h1>;
```

---

### Совет 5: Проверка массивов

Для массивов проверяйте длину:
```javascript
{gallery?.images?.length > 0 && (
  <div>
    {gallery.images.map((img, i) => (
      <img key={i} src={img} alt="" />
    ))}
  </div>
)}
```

---

## 🎨 Часть 5: Настройка внешнего вида

### Изменить цвет кнопки

```javascript
<AdminPanel theme={{ primaryColor: '#4CAF50' }} />
```

**Популярные цвета:**
- Синий: `#1976D2`
- Зеленый: `#4CAF50`
- Красный: `#F44336`
- Оранжевый: `#FF5722`
- Фиолетовый: `#9C27B0`

---

### Изменить позицию кнопки

Откройте файл:
```
universal-admin-package/src/components/AdminTrigger/AdminTrigger.jsx
```

Найдите стили (примерно строка 20):
```javascript
position: 'fixed',
bottom: '20px',  // Отступ снизу
right: '20px',   // Отступ справа
```

Измените на:
```javascript
bottom: '100px',  // Выше
left: '20px',     // Слева
```

---

### Изменить текст кнопки

В том же файле найдите:
```javascript
⚙️ Админ-панель
```

Измените на:
```javascript
✏️ Редактировать
```

---

## 🔐 Часть 6: Смена пароля

### Шаг 8.6.1: Откройте файл

```
universal-admin-package/src/components/AdminDashboard/AdminDashboard.jsx
```

### Шаг 8.6.2: Найдите строку с паролем

Примерно строка 20:
```javascript
const ADMIN_PASSWORD = 'admin123';
```

### Шаг 8.6.3: Измените пароль

```javascript
const ADMIN_PASSWORD = 'мой-новый-пароль-2024';
```

### Шаг 8.6.4: Сохраните файл

Теперь для входа нужен новый пароль!

---

## 📊 Часть 7: Структура данных в Firebase

Ваши данные хранятся так:

```
Firestore Database
└── pages (коллекция)
    ├── home (документ)
    │   ├── hero (объект)
    │   │   ├── title: "..."
    │   │   ├── subtitle: "..."
    │   │   └── description: "..."
    │   └── gallery (объект)
    │       ├── title: "..."
    │       └── images: ["url1", "url2"]
    ├── about (документ)
    └── contact (документ)
```

**Путь к данным:**
```
pages.home.hero.title
└─┬─┘ └┬─┘ └┬─┘ └─┬─┘
  │    │    │     └─ Поле
  │    │    └─────── Секция
  │    └──────────── Страница
  └───────────────── Коллекция
```

---

## ✅ Что вы узнали

- ✅ Как добавить админ-панель на другие страницы
- ✅ Как создать новую страницу
- ✅ Какие типы полей существуют
- ✅ Как использовать данные в коде
- ✅ Полезные советы и трюки
- ✅ Как настроить внешний вид
- ✅ Как сменить пароль

---

## 🚀 Следующий шаг

Переходите к файлу: **ИНСТРУКЦИЯ_9_ПРОБЛЕМЫ.md**

Там описаны решения частых проблем.
